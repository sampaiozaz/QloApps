# Dokka Documentation Generator

Documentation for `contact-health-service-kotlin` can be generated using Dokka.

## Generating HTML Documentation

Run the following command in the service root directory:

```bash
./gradlew dokkaHtml
```

The output HTML documentation will be generated in:
`build/dokka/html/index.html`
